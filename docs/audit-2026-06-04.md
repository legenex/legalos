# LegalOS — Static Bug Audit (2026-06-04)

Multi-agent static review: 15 subsystem reviewers + an independent adversarial verifier per finding. No local build was available (codespace has no deps/.env/DB), so this is static analysis only — not runtime-verified.

**Result:** 58 raw findings → **50 confirmed**, 1 uncertain, 7 rejected.  
Confirmed by severity: critical 1, high 12, medium 20, low 16, nit 1.


## CRITICAL (1)

### 1. Sites collection: brand_identity, legal_*, typography_*, and new brand_* columns exist in no migration (schema drift, breaks prod)
- **Where:** `src/migrations/20260513_221103_init.ts:66-95`  ·  **Subsystem:** Migrations vs schema drift  ·  **Category:** schema-migration  ·  **Reviewer confidence:** high
- **Problem:** src/collections/Sites.ts declares many fields that no committed migration ever creates as columns. The init migration's `sites` table (and its JSON snapshot, public.sites) has exactly these columns: id, name, slug, status, vertical, tagline, default_phone, default_phone_tel, org_name, org_address, support_email, brand_logo_url, brand_favicon_url, brand_primary, brand_accent, brand_surface, brand_ink, brand_muted, brand_success, brand_warning, brand_danger, brand_font_heading, brand_font_body, default_tone, default_disclaimer_md, archived_at, created_at, updated_at. Sites.ts additionally declares: brand_display_name, brand_short_name, brand_logo_url_dark, brand_tagline_brand (brand group); legal_copyright, legal_tcpa_text, legal_privacy_url, legal_terms_url, legal_default_disclaimer (legal group); typography_headline_font, typography_body_font, typography_base_size (typography group); and brand_identity (jsonb). Grepping every file in src/migrations confirms none of these column names are created by any migration. Because payload.config.ts disables schema auto-push in production (only src/migrations/ are applied), these columns will not exist on the prod DB. Payload's queries enumerate every collection column, so any SELECT on `sites` will throw 'column ... does not exist' — exactly the failure mode the 20260529_060000 migration's own header describes. Note brand_identity is load-bearing: the funnel builder treats Site.brand_identity as the source of truth, and the 20260529_060000 comment explicitly relies on it. A migration adding all these columns is missing.
- **Fix:** Run `pnpm payload migrate:create sites_brand_identity_legal_typography` against an up-to-date schema, or hand-write an idempotent migration that ADDs the missing columns: brand_display_name, brand_short_name, brand_logo_url_dark, brand_tagline_brand, legal_copyright, legal_tcpa_text, legal_privacy_url, legal_terms_url, legal_default_disclaimer, typography_headline_font, typography_body_font, typography_base_size, and brand_identity jsonb — all nullable (or with the same defaults the collection uses) so existing rows are unaffected. Register it in src/migrations/index.ts and run `pnpm payload migrate` on the server.
- **Verifier:** The claim is REAL and reachable. Verified directly:

1) src/collections/Sites.ts declares fields absent from every migration. Lines 95-115 define a `brand` group whose newer sub-fields `display_name`, `short_name`, `logo_url_dark`, `tagline_brand` flatten (Payload postgres adapter) to columns `brand_display_name`, `brand_short_name`, `brand_logo_url_dark`, `brand_tagline_brand`. Lines 117-127 define a `legal` group (`copyright`, `tcpa_text`, `privacy_url`, `terms_url`, `default_disclaimer` -> `legal_copyright`, `legal_tcpa_text`, `legal_privacy_url`, `legal_terms_url`, `legal_default_disclaime


## HIGH (12)

### 2. `extra` custom form fields are parsed and validated but never forwarded to the pipeline or persisted
- **Where:** `src/app/api/leads/route.ts:38-122`  ·  **Subsystem:** Lead pipeline core  ·  **Category:** data-integrity  ·  **Reviewer confidence:** high
- **Problem:** The public POST body schema accepts `extra: z.record(z.unknown()).optional()` with a comment stating these custom fields are "Stored as JSON on the lead row by the pipeline" (route.ts lines 34-38). LeadForm.tsx actually collects every non-contact, non-attribution form field into `extra` and sends it (LeadForm.tsx lines 127-152). But the call to `runLeadPipeline` (route.ts lines 107-122) never passes `data.extra`, `LeadCaptureInput` (run.ts lines 14-41) has no `extra` field, the `payload.create` call (run.ts lines 90-103) never writes it, and the Leads collection has no `extra` field at all. So all custom form data — explicitly called out in the comment as including `case_type`, `accident_date`, and `agreed_to_tcpa` — is silently discarded. Losing a TCPA consent flag captured via a custom field is a compliance/data-integrity problem, not just lost analytics.
- **Fix:** Thread `extra` through: add `extra?: Record<string, unknown>` to LeadCaptureInput, pass `extra: data.extra` from the route, and persist it on the lead (e.g. merge into quiz_answers or add a dedicated `extra` JSON field on the Leads collection and write it in payload.create). Add the matching Payload field + run generate:types.
- **Verifier:** Verified the full chain end-to-end; the bug is real and reachable.

1. CLIENT collects and sends `extra`. src/components/blocks/LeadForm.tsx line 30 defines `const CONTACT_KEYS = new Set(['first_name', 'last_name', 'email', 'phone', 'state', 'zip'])`. The submit handler (lines 128-144) walks every FormData entry, skips `attr_*` attribution keys, routes the 6 contact keys into `contact`, and puts everything else into `extra`. Line 152 sends it: `extra: Object.keys(extra).length > 0 ? extra : undefined`, included in the POST body at line 162. Custom fields come from `block.form_fields` rendered 

### 3. No fetch timeout on synchronous in-request integration calls — a hung remote stalls the lead response
- **Where:** `src/lib/integrations/meta-capi.ts:70-74`  ·  **Subsystem:** External integrations  ·  **Category:** error-handling  ·  **Reviewer confidence:** high
- **Problem:** None of the integration fetch calls (Meta CAPI here, plus TrustedForm, Jornaya, TrueCall) pass an AbortController/signal or any timeout. Per CLAUDE.md and src/lib/lead-pipeline/run.ts the lead pipeline runs SYNCHRONOUSLY inside the POST /api/leads request, and these four tasks are awaited via `await Promise.allSettled([...])` (run.ts:427) before the route returns. If any of these remote endpoints accepts the TCP connection but never responds (a common failure mode for third-party APIs), the fetch hangs indefinitely, `Promise.allSettled` never settles, and the public lead request blocks until the platform/proxy kills it — the caller never gets back the `event_id`, so the client pixel can't fire its deduplicated event (breaking the Meta dedupe contract) and the lead capture appears to fail. A bounded `AbortSignal.timeout(...)` is needed on each blocking integration. (HLR in hlr.ts is fire-and-forget/async via `void` in run.ts:456 so it is less critical, but the same omission applies there too.)
- **Fix:** Add a timeout to each blocking integration fetch, e.g. pass `signal: AbortSignal.timeout(8000)` to the fetch options in meta-capi.ts, trustedform.ts, jornaya.ts, and truecall.ts, and treat the resulting AbortError as a normal `{ ok: false, error: 'timeout' }` in the existing catch block. This bounds the synchronous lead request regardless of third-party latency.
- **Verifier:** Verified by reading the cited files directly. meta-capi.ts:70-74 matches the snippet verbatim: `const resp = await fetch(\`https://graph.facebook.com/v21.0/${pixelId}/events\`, { method:'POST', headers:{...}, body: JSON.stringify(payload) })` — no `signal`/timeout in the options. The surrounding try/catch (lines 69-80) only catches thrown errors; a TCP connection that is accepted but never produces a response leaves `await fetch` / `await resp.json()` pending forever. Same omission confirmed in trustedform.ts:32-40, jornaya.ts:25, truecall.ts:54-62, the inline tiktok fetch (run.ts:240) and ga4

### 4. ?site= and ?preview=1 preview bypass any tenant boundary — any authenticated user can view another Site's draft/paused content
- **Where:** `src/app/(public)/[[...slug]]/page.tsx:126-128, 137-152, 168-174`  ·  **Subsystem:** Site resolution & public routing  ·  **Category:** tenant-isolation  ·  **Reviewer confidence:** high
- **Problem:** The preview channels only verify that SOME user is authenticated, never that the user is bound to the Site being previewed. `previewSiteSlug = authedUser ? rawPreviewSiteSlug : null` gates on `Boolean(authedUser)` only; then `siteId` is resolved from the arbitrary slug and `isAdminPreview` relaxes the status filter (draft/paused/scheduled all render). The codebase already has `isBoundToSite(user, siteId)` and uses it to gate the admin site layout (src/app/(app)/admin/sites/[slug]/layout.tsx:33) and test-capture (src/app/api/legalos/test-capture/route.ts:66), but it is NOT called here. Concrete exploit: an `analyst` bound only to Site A logs in, then requests `https://any-host/?site=site-b-slug` (or `https://site-b-host/?preview=1`) and sees Site B's unpublished/paused pages, including draft legal/marketing content of a different tenant. This is a cross-tenant data-exposure bug.
- **Fix:** After resolving the preview siteId, require the user to be bound to it: for the ?site= path, set `previewSiteSlug` only if `isBoundToSite(authedUser, resolvedSiteId)`; for ?preview=1 compute `isAuthedAdminPreview = previewMode && isBoundToSite(authedUser, siteId)` instead of `Boolean(authedUser)`. Fall back to the normal public (published-only) render when the user is not bound.
- **Verifier:** The bug is real and reachable. In src/app/(public)/[[...slug]]/page.tsx the preview channels gate ONLY on authentication, never on tenant binding.

Path 1 (?site=<slug>): middleware (src/middleware.ts:24-29) forwards x-legalos-preview-site. Line 126 `const wantsPreview = previewMode || Boolean(rawPreviewSiteSlug)`; line 127 `const authedUser = wantsPreview ? await getCurrentUser() : null` returns ANY authenticated user; line 128 `const previewSiteSlug = authedUser ? rawPreviewSiteSlug : null` gates only on Boolean(authedUser). Lines 137-144 then resolve siteId from the arbitrary slug with `ove

### 5. Brands UI domain delete uses the wrong pleskIsConfigured, so it never unprovisions the nginx vhost/cert
- **Where:** `src/app/(app)/admin/(top)/brands/domains/actions.ts:11, 163-167`  ·  **Subsystem:** Domain provisioning / Plesk / SSL / DNS  ·  **Category:** correctness  ·  **Reviewer confidence:** high
- **Problem:** deletePoolDomain imports pleskIsConfigured from '@/lib/plesk/client', whose implementation returns Boolean(process.env.PLESK_API_URL && process.env.PLESK_API_KEY). Under the current provisioning model the Plesk REST API is no longer used; provision-domain.ts says PLESK_API_URL/PLESK_API_KEY are not required, and .env.example ships PLESK_API_KEY blank. So client.pleskIsConfigured() returns false in production, and the `if (pleskId && pleskIsConfigured())` guard is never true — unprovisionDomainInPlesk() is skipped. Deleting a custom domain from the Brands UI therefore deletes the DB row but leaves the per-tenant nginx config at /etc/nginx/conf.d/legalos-tenants/<host>.conf and the Let's Encrypt cert orphaned on the server, still serving the host. The sibling path removeDomain() in settings/domains/actions.ts imports pleskIsConfigured from '@/lib/plesk/provision-domain' (which returns true by default), so it DOES unprovision. The two delete paths behave inconsistently for the same operation.
- **Fix:** Import pleskIsConfigured from '@/lib/plesk/provision-domain' (the filesystem/CLI-based model) instead of '@/lib/plesk/client', matching removeDomain() in settings/domains/actions.ts, so the Brands delete path actually removes the nginx vhost and revokes the cert.
- **Verifier:** The bug is real and reachable. In src/app/(app)/admin/(top)/brands/domains/actions.ts line 11, deletePoolDomain imports pleskIsConfigured from '@/lib/plesk/client'. That implementation (src/lib/plesk/client.ts:142-143) is `Boolean(process.env.PLESK_API_URL && process.env.PLESK_API_KEY)`. The new provisioning model deliberately abandoned the Plesk REST API: src/lib/plesk/provision-domain.ts (lines 216-224) states "Provisioning no longer requires PLESK_API_URL / PLESK_API_KEY" and its own pleskIsConfigured returns `true` by default (only false if LEGALOS_DISABLE_PROVISIONING=true). .env.example 

### 6. Structured import 'no matches' fallback never fires when CSS yields a brand palette, producing a content-less page
- **Where:** `src/lib/builder/html-to-structured-blocks.ts:555`  ·  **Subsystem:** Page builder lib  ·  **Category:** correctness  ·  **Reviewer confidence:** high
- **Problem:** extractStructuredFromHtml pushes up to TWO leading style blocks: an 'Imported brand palette' override (line 487, pushed whenever buildBrandOverrideCss returns non-null) AND a 'Page styles (CSS)' block (line 494, pushed whenever css is non-empty). The final 'no structural matches at all -> dump the whole body as one block' guard, however, only checks `blocks.length === (css ? 1 : 0)`. When the uploaded CSS contains :root brand vars (so the override block IS emitted) and the HTML has no semantic <section>/<nav>/<footer>/<header> and no content-heavy `body > div`, blocks.length is 2, the guard is false, and the whole-body fallback is skipped. The result is a Page created (html-import-action.ts line 128, overrideAccess:false) containing only two <style> blocks and ZERO content blocks -- a silent content-loss import. The sibling extractBlocksFromHtml in html-to-blocks.ts does not hit this because it never emits a brand-override block (only the single CSS block), so its identical `=== (css ? 1 : 0)` guard is correct there. The guard must count the actual number of scaffold/style blocks pushed (override + css), not assume at most one.
- **Fix:** Track how many scaffold blocks were pushed and compare against that. e.g. `const scaffoldCount = (overrideCss ? 1 : 0) + (css ? 1 : 0)` set just after the two pushes, then `if (blocks.length === scaffoldCount) { ... }`. This restores the whole-body fallback whenever no content/structured block was produced.
- **Verifier:** Read /workspaces/legalos/src/lib/builder/html-to-structured-blocks.ts (extractStructuredFromHtml, lines 453-567). Two leading style blocks can be pushed BEFORE any content: line 486-492 pushes a 'Imported brand palette' override block whenever `buildBrandOverrideCss(brand)` returns non-null, and line 494-500 pushes a 'Page styles (CSS)' block whenever `css` is non-empty. I verified buildBrandOverrideCss in extract-brand-tokens.ts (line 140-162): it returns non-null whenever ANY brand token is found (`if (lines.length === 0) return null` — so any extracted `:root` var or font produces a string)

### 7. LeadForm has noValidate but performs zero client-side validation, so required fields (incl. TCPA consent) are never enforced
- **Where:** `src/components/blocks/LeadForm.tsx:234, 101-156`  ·  **Subsystem:** Block renderer & lead form  ·  **Category:** compliance  ·  **Reviewer confidence:** high
- **Problem:** The <form> sets noValidate, which disables the browser's HTML5 constraint validation. The onSubmit handler does no manual validation at all (no formRef.current.checkValidity(), no per-field checks) before building the payload and POSTing. Meanwhile the server schema (ContactSchema in src/app/api/leads/route.ts) makes every contact field .optional() and the pipeline never enforces required fields or consent (no consent/tcpa/required check in run.ts). Net effect: every `required` attribute on inputs/selects/textarea/checkbox (including a `required` TCPA-consent checkbox authored via form_fields) is a decorative no-op. A user can submit a completely blank form, or submit without checking the consent box, and an empty/consent-less lead is persisted. For a legal lead-gen product this is a real form-validation and compliance defect, not cosmetic.
- **Fix:** Either remove `noValidate` (let the browser enforce `required`/`type=email`), or in onSubmit call `if (!formRef.current.checkValidity()) { formRef.current.reportValidity(); setPending(false); return }` before building the payload. Additionally enforce required contact fields / consent on the server so the API cannot persist an empty or consent-less lead.
- **Verifier:** Both halves of the claim are accurate after reading the actual code.

CLIENT (src/components/blocks/LeadForm.tsx):
- Line 234: `<form ref={formRef} onSubmit={onSubmit} ... noValidate>` — `noValidate` disables HTML5 constraint validation.
- onSubmit (lines 101-191): `e.preventDefault()` then `setPending(true)` then directly `const fd = new FormData(formRef.current)` — no `formRef.current.checkValidity()`, no `reportValidity()`, and no per-field manual checks anywhere before `fetch('/api/leads')` at line 159.
- The `required` attributes ARE rendered on fields: Input (line 296, applied at line 42

### 8. MarkdownLite injects unescaped source via dangerouslySetInnerHTML (stored XSS in prose/faq/disclosure/footer)
- **Where:** `src/components/blocks/BlockRenderer.tsx:235-249`  ·  **Subsystem:** Block renderer & lead form  ·  **Category:** security  ·  **Reviewer confidence:** high
- **Problem:** MarkdownLite takes raw `source`, applies a few inline-markdown regex replacements, and renders the result with dangerouslySetInnerHTML WITHOUT first HTML-escaping the base text and WITHOUT running it through sanitizeHtml(). Any raw HTML in the source passes through verbatim: e.g. a field value containing `<img src=x onerror=alert(document.cookie)>` executes. This path feeds Prose.markdown (line 777), Faq answer (line 639), Disclosure.markdown (line 821), and SiteFooter legal_md / site.default_disclaimer_md (line 879) — all author/AI-supplied content that is substituted upstream, none of it sanitized. The link-rendering rule also inserts the href ($2) unescaped, so `[x](" onmouseover="alert(1))` breaks out of the attribute, and `[x](javascript:alert(1))` yields a javascript: link.
- **Fix:** HTML-escape the paragraph text before applying the inline-markdown replacements (escape &, <, >, "), and validate/escape the link href (reject non-http(s)/mailto/tel schemes, escape quotes). Or route MarkdownLite output through sanitizeHtml() as a defense-in-depth pass.
- **Verifier:** Verified directly in src/components/blocks/BlockRenderer.tsx. MarkdownLite (lines 235-249) takes raw `source`, applies inline-markdown regex replacements, and renders via dangerouslySetInnerHTML with NO HTML-escaping and NO sanitizeHtml() pass:
  const withInline = p
    .replace(/\*\*(.+?)\*\*/g, '<strong>$1</strong>')
    .replace(/\*(.+?)\*/g, '<em>$1</em>')
    .replace(/\[([^\]]+)\]\(([^)]+)\)/g, '<a href="$2" style="color:var(--site-primary);">$1</a>')
    .replace(/\n/g, '<br />')
  return <p ... dangerouslySetInnerHTML={{ __html: withInline }} />
Any raw HTML in source (e.g. <img src=x

### 9. sanitizeHtml misses unquoted on*= handlers, iframes, and javascript: URLs (custom_html / embed XSS bypass)
- **Where:** `src/components/blocks/BlockRenderer.tsx:228-232`  ·  **Subsystem:** Block renderer & lead form  ·  **Category:** security  ·  **Reviewer confidence:** high
- **Problem:** sanitizeHtml only strips <script> blocks and on*= handlers that are wrapped in double or single quotes. It does NOT strip unquoted event handlers (e.g. `<img src=x onerror=alert(1)>` has no quotes around the handler value), nor does it remove <iframe>/<object>, <svg onload=...>, or `javascript:`/`data:` URLs in href/src. Both CustomHtml (line 812) and Embed (line 804) feed author-supplied HTML through this regex and then dangerouslySetInnerHTML, so an unquoted-handler payload renders and executes. The sanitizer gives a false sense of safety.
- **Fix:** Replace the hand-rolled regex with a real sanitizer (e.g. DOMPurify / sanitize-html) with an allowlist of tags/attributes/URL schemes, or at minimum also strip unquoted on*= handlers (`/\son[a-z]+\s*=\s*[^\s>]+/gi`), <iframe>/<object>/<embed> tags, and javascript:/data: URL schemes.
- **Verifier:** The cited evidence is verbatim-accurate. src/components/blocks/BlockRenderer.tsx:228-232 defines sanitizeHtml with exactly three replacements: strip <script>...</script>, strip double-quoted on*= handlers (/\son[a-z]+\s*=\s*"[^"]*"/gi), and strip single-quoted on*= handlers (/\son[a-z]+\s*=\s*'[^']*'/gi). Nothing else. This output feeds dangerouslySetInnerHTML in both Embed (line 804: `<div dangerouslySetInnerHTML={{ __html: sanitizeHtml(html) }} />`) and CustomHtml (line 812). The html comes straight from author-supplied block fields: Pages.ts:53-57 (embed block, `html` textarea, required) an

### 10. Site-scoped access helpers don't normalize populated siteBindings.site, breaking tenant filters
- **Where:** `src/access/index.ts:17-51`  ·  **Subsystem:** Access control & multi-tenancy  ·  **Category:** tenant-isolation  ·  **Reviewer confidence:** high
- **Problem:** siteIdsForUser() returns user.siteBindings.map((b) => b.site) as-is and feeds it into the `{ site: { in: siteIds } }` filter returned by siteScopedRead/Write/Admin. But in the access-control context `req.user` comes from payload.auth() at the config default depth, which POPULATES the `site` relationship as an object ({ id, name, slug, ... }), not a raw id. The codebase itself proves this everywhere else: src/lib/auth.ts isBoundToSite has an explicit comment 'Handles bindings whose site is either a populated object or a raw id' and does `typeof b.site === 'object' ? b.site.id : b.site` with Number() coercion; src/hooks/site-cascade.ts (line 66) and src/app/(app)/admin/(top)/settings/users/page.tsx (line 69-73) do the same normalization. The access helpers are the only readers that do NOT normalize. As a result the constraint becomes `{ site: { in: [{id,...},{id,...}] } }`, which Payload cannot match against a scalar relationship id — site-scoped editors/analysts get either zero rows on read (over-restrictive: their own tenant's Pages/Leads/Numbers/etc. disappear in raw /cms and via /api/*) or rejected writes. The custom /admin UI mostly uses overrideAccess + isBoundToSite so it masks this, but the raw /cms admin and the public /api/* REST/GraphQL surface (documented as in-use for fields not yet in the custom UI) enforce these rules and are broken for every non-super-admin.
- **Fix:** Normalize each binding's site to a scalar id (and coerce type) before building the filter, mirroring isBoundToSite/auth.ts: `.map((b) => (b.site && typeof b.site === 'object' ? (b.site as { id: string | number }).id : b.site))`. Apply the same in the rank filter is unaffected (b.role stays scalar).
- **Verifier:** The bug is real and matches the cited code. src/access/index.ts:17-24 siteIdsForUser does `return user.siteBindings.filter((b) => rank[b.role] >= min).map((b) => b.site)` with NO normalization, and its UserLike type (line 6) wrongly declares `siteBindings?: Array<{ site: string | number; ... }>`. That array is fed verbatim into the filter at lines 30-32/39-41/48-50: `return { site: { in: siteIds } }`.

siteBindings.site is a Payload `relationship` field to `sites` (src/collections/Users.ts:78: `{ name: 'site', type: 'relationship', relationTo: 'sites', required: true }`). req.user in access co

### 11. createPageFromUrl performs server-side fetch of arbitrary user URL (SSRF)
- **Where:** `src/app/(app)/admin/sites/[slug]/pages/new/ai-clone-action.ts:52-90`  ·  **Subsystem:** Page-builder server actions  ·  **Category:** security  ·  **Reviewer confidence:** high
- **Problem:** createPageFromUrl takes a fully user-controlled sourceUrl, normalizes it to http(s), and fetch()es it server-side with redirect: 'follow' and no host/IP allow-list or private-range block. Any authenticated user can point it at internal/cloud-metadata endpoints (e.g. http://169.254.169.254/latest/meta-data/, http://127.0.0.1:3000/admin, internal Plesk/Postgres/Redis hosts on the box). Reachability and limited content are observable to the caller: on non-2xx the action returns `Source URL returned ${res.status} ${res.statusText}`, and the SPA guard returns `${textOnly.length} chars of visible text`, both of which confirm internal services and can echo fragments. redirect: 'follow' also lets an external page 302 into an internal address, defeating any naive host check on the original URL.
- **Fix:** Before fetching, parse the URL, reject non-http(s) schemes, resolve the hostname and reject loopback/link-local/private/reserved ranges (127.0.0.0/8, 10/8, 172.16/12, 192.168/16, 169.254/16, ::1, fc00::/7) and localhost; re-validate after each redirect (use redirect: 'manual' and re-check the Location host, or use an SSRF-safe fetch wrapper). Optionally require the user to be bound to args.siteId first.
- **Verifier:** Verified against /workspaces/legalos/src/app/(app)/admin/sites/[slug]/pages/new/ai-clone-action.ts. The reviewer's snippet matches exactly. Line 52: `let sourceUrl = (args.sourceUrl || '').trim()`; line 54: `if (!/^https?:\/\//i.test(sourceUrl)) sourceUrl = 'https://' + sourceUrl` — the only scheme check just decides whether to prepend https://, it rejects nothing. Lines 72-75: `const res = await fetch(sourceUrl, { headers: {...}, redirect: 'follow' })` — no URL parsing, no hostname/IP resolution, no private-range/loopback/link-local block, no allow-list. A repo-wide grep for ssrf/169.254/127.

### 12. System Health page exposes infra secrets to any authenticated user (no super_admin gate)
- **Where:** `src/app/(app)/admin/(top)/system/page.tsx:43-48`  ·  **Subsystem:** System health / self-check / media / misc API  ·  **Category:** security  ·  **Reviewer confidence:** high
- **Problem:** SystemPage calls runSystemReport() and renders the full report with no super_admin check. The root admin layout (src/app/(app)/admin/layout.tsx) only does `if (!user) redirect(...)`, and the (top) layout does no role check at all, so any authenticated user — including an `analyst` or `editor` bound to a single tenant Site — can navigate directly to /admin/system and read LegalOS-wide infrastructure details. runSystemReport surfaces: the Postgres connection string (host/db/user, only password masked) via maskDbUrl, the Anthropic API key prefix (first 12 chars) via checkAnthropic's `key_prefix`, the Plesk API URL + hostname + version, the git commit SHA/message/date, and server internals (hostname, PID, platform, memory). This is the same class of LegalOS-wide integration data that the sibling pages settings/integrations/page.tsx and settings/users/page.tsx explicitly gate behind `if (!me.super_admin)` — those pages establish the required convention, which the system pages violate. This is a cross-tenant info-disclosure / missing-access-control bug. (Note: the duplicate copy at src/app/(app)/admin/(top)/settings/system/page.tsx has the identical gap.)
- **Fix:** At the top of SystemPage (and the settings/system copy), fetch the current user and gate on super_admin, mirroring settings/integrations/page.tsx: `const me = await getCurrentUser(); if (!me) redirect('/sign-in?next=/admin/system'); if (!me.super_admin) notFound()` (or redirect). The report contents are LegalOS-wide infra data and must not be visible to per-tenant users.
- **Verifier:** The bug is real and reachable. SystemPage at src/app/(app)/admin/(top)/system/page.tsx:43-44 calls `const report = await runSystemReport()` with no user fetch and no role check; the identical duplicate at src/app/(app)/admin/(top)/settings/system/page.tsx:43-44 does the same. No layer above it gates super_admin: root admin layout (src/app/(app)/admin/layout.tsx:18-19) only does `if (!user) redirect('/sign-in?redirect=/admin/overview')`; the (top) layout (src/app/(app)/admin/(top)/layout.tsx:5-12) just reads `user?.email` for the Sidebar; the settings layout renders only a sub-nav; and middlewa

### 13. Six funnel-builder collections have no migration — tables and locked-documents FK columns are absent in production
- **Where:** `src/payload.config.ts:60-65`  ·  **Subsystem:** Migrations vs schema drift  ·  **Category:** schema-migration  ·  **Reviewer confidence:** high
- **Problem:** payload.config.ts registers six funnel-builder collections — FunnelLandingPages (funnel-landing-pages), FunnelLpDeployments (funnel-lp-deployments), FunnelQuizzes (funnel-quizzes), FunnelQuizDeployments (funnel-quiz-deployments), FunnelAdvertorials (funnel-advertorials), FunnelAdvertorialDeployments (funnel-advertorial-deployments). No migration in src/migrations/ creates any of their tables (grep for `create table "funnel` returns nothing; the only file matching 'funnel' is 20260525_..._funnel_domain, which merely adds domain_id to quizzes/landing_pages). Additionally, the payload_locked_documents_rels table created in init has FK columns only for the original collections (audit_log_id, blog_posts_id, domains_id, landing_pages_id, leads_id, numbers_id, pages_id, quizzes_id, shared_legal_templates_id, sites_id, tracking_configs_id, users_id) — none for the funnel collections, and no later migration adds them. Because production disables schema auto-push (payload.config.ts), opening any funnel builder, or any Payload query that enumerates all collection FK rels (the exact failure the 20260530 media migration was written to fix), will throw 'relation/column does not exist'. The funnel feature is non-functional on a migrations-only prod DB.
- **Fix:** Generate a migration (`pnpm payload migrate:create funnel_builder_collections`) that creates all six funnel tables (plus their nested array/relationship child tables) and adds the matching FK columns (funnel_landing_pages_id, funnel_lp_deployments_id, funnel_quizzes_id, funnel_quiz_deployments_id, funnel_advertorials_id, funnel_advertorial_deployments_id) + indexes to payload_locked_documents_rels, mirroring the media_id fix in 20260530. Register it in src/migrations/index.ts and run `pnpm payload migrate` on the server.
- **Verifier:** Confirmed by reading the actual files. (1) payload.config.ts lines 60-65 register six full Payload CollectionConfig collections (FunnelLandingPages 'funnel-landing-pages', FunnelLpDeployments, FunnelQuizzes, FunnelQuizDeployments, FunnelAdvertorials, FunnelAdvertorialDeployments). FunnelLandingPages.ts is a normal `CollectionConfig` with real fields (slug 'funnel-landing-pages', required text fields, a json field) — no `db:false`/virtual flag — so Payload needs a backing table `funnel_landing_pages` etc. (2) `grep 'create table \"funnel'` across src/migrations/ returns nothing; the only 'funne


## MEDIUM (20)

### 14. Client-supplied fbc/fbp cookies are silently dropped server-side, degrading Meta CAPI match/dedupe
- **Where:** `src/lib/lead-pipeline/attribution.ts:20-32`  ·  **Subsystem:** Lead pipeline core  ·  **Category:** data-integrity  ·  **Reviewer confidence:** high
- **Problem:** LeadForm.tsx reads the Meta `_fbc` and `_fbp` cookies and sends them in the `attribution` object (LeadForm.tsx lines 57-60, and they are even listed as hidden inputs at lines 250-251). The public POST handler runs this object through `pickAttributionFromObject` (route.ts line 102), which only copies keys present in `ATTRIBUTION_KEYS`. That array does NOT include `fbc` or `fbp`, so both are stripped before reaching the pipeline. As a result `attribution.fbp` is always undefined when Meta CAPI fires (run.ts line 219) and the real `_fbc` cookie value is discarded. The pipeline only ever has a derived `fbc` (run.ts lines 84-86) built from the URL `fbclid` using the server's current time as the cookie creation_time, which is less accurate than the browser's real `_fbc` cookie and missing entirely when there is no `fbclid` in the URL. `fbp` is a core Meta match parameter, so this directly weakens the Pixel+CAPI dedupe/match contract this subsystem is supposed to uphold.
- **Fix:** Add 'fbc' and 'fbp' (and likely 'gclid' is already present) to ATTRIBUTION_KEYS so the client-supplied Meta cookies survive the server-side filter and reach sendMetaCAPIEvent. Then the deriveFbc fallback at run.ts:84 only kicks in when no real _fbc cookie was sent.
- **Verifier:** The full client-to-server data flow confirms the bug.

CLIENT (src/components/blocks/LeadForm.tsx):
- captureAttribution() reads the Meta cookies: `const fbc = document.cookie.split('; ').find((c) => c.startsWith('_fbc='))?.split('=')[1]` / `const fbp = ...startsWith('_fbp=')...` (lines 57-58), then `if (fbc) out.fbc = fbc` / `if (fbp) out.fbp = fbp` (lines 59-60).
- Hidden inputs `attr_fbc` / `attr_fbp` exist (lines 250-251) and are hydrated in useEffect (lines 92-99).
- onSubmit collects every `attr_*` field into `attribution` (lines 109-114) and POSTs it as `attribution` to /api/leads (line

### 15. Shared-template markdown renders unescaped HTML and allows javascript: links (stored-XSS surface)
- **Where:** `src/app/(public)/[[...slug]]/page.tsx:458-495`  ·  **Subsystem:** Site resolution & public routing  ·  **Category:** security  ·  **Reviewer confidence:** medium
- **Problem:** markdownToHtml escapes heading text via escapeHtml, but paragraph, list, and inline text are passed through inlineMd which performs NO HTML escaping of the underlying text before injecting via dangerouslySetInnerHTML (line 449). Any raw `<script>`/`<img onerror=>` in a SharedLegalTemplate body, a per-Site override, or a substituted `{{site.*}}` value (e.g. site.name/org_name set in admin) is rendered verbatim. Additionally the link rule `\[([^\]]+)\]\(([^)]+)\)` -> `<a href="$2">` does not validate the URL, so a template containing `[click](javascript:alert(1))` produces a javascript: anchor. This is admin-authored content so impact is limited to write-capable users, but it is a real injection sink on the public site (and tenant admins/editors are not necessarily fully trusted across a multi-tenant install).
- **Fix:** escapeHtml the raw text in every branch (paragraphs, list items) before applying inline markdown replacements, and reject/strip non-http(s) URLs in the link rule (e.g. only emit href when /^https?:\/\//.test(url) or a relative path).
- **Verifier:** Verified in src/app/(public)/[[...slug]]/page.tsx. The reviewer's snippet is exact.

1) No escaping on paragraph/list/inline text. markdownToHtml (line 458) only escapes HEADING text: line 467 uses escapeHtml(h[2]). But the paragraph branch (line 485) emits `<p ...>${inlineMd(trimmed.replace(/\n/g, '<br />'))}</p>` and the list branches (lines 473, 481) emit `<li>${inlineMd(m[1])}</li>` — all pass raw text straight into inlineMd. inlineMd (lines 490-495) performs only `**`->strong, `*`->em, and `[]()`->anchor replacements; it calls escapeHtml on nothing. The result is injected via dangerouslyS

### 16. Brand color/font values are injected raw into a <style> tag without sanitization (CSS-injection / style breakout)
- **Where:** `src/app/(public)/layout.tsx:32-49, 86`  ·  **Subsystem:** Site resolution & public routing  ·  **Category:** security  ·  **Reviewer confidence:** medium
- **Problem:** siteStyleVars interpolates Site.brand fields (primary, accent, surface, ink, font_heading, etc.) directly into a CSS string that is then emitted via `<style dangerouslySetInnerHTML>`. None of the values are validated. A brand value such as `red}</style><script>...` (or `red;} body{display:none` ) breaks out of the declaration/`<style>` element on every public page of that Site. Admin-authored, so lower severity, but it is an unsanitized sink reaching the public DOM.
- **Fix:** Validate each brand token against an allowlist (hex/rgb/hsl color regex for colors; a font-name regex for fonts) and fall back to the default when it doesn't match, before interpolating into the style string.
- **Verifier:** The claim is accurate and the sink is real and reachable. In src/app/(public)/layout.tsx, `siteStyleVars` (lines 32-50) interpolates brand fields verbatim into a CSS string, e.g. line 35 `--site-primary:${b?.primary ?? '#0B1F3A'};` ... line 43 `--site-font-heading:${b?.font_heading ?? 'Inter'},...`, with zero validation. That string is emitted on every public page via line 86: `{site ? <style dangerouslySetInnerHTML={{ __html: siteStyleVars(site) }} /> : null}`. React's dangerouslySetInnerHTML does NOT escape `<`, `>`, or `/`, so a value like `red}</style><script>...</script>` closes the <styl

### 17. Tenant nginx serves www.<host> over TLS but the cert is only issued for the apex host (www TLS mismatch)
- **Where:** `src/lib/plesk/provision-domain.ts:97-100, 128-133`  ·  **Subsystem:** Domain provisioning / Plesk / SSL / DNS  ·  **Category:** correctness  ·  **Reviewer confidence:** medium
- **Problem:** renderTenantNginxConfig writes `server_name ${host} www.${host};` on the :443 block and points ssl_certificate at LE_CERT_DIR/${host}/fullchain.pem. But issueLetsEncryptCert requests the cert with only `-d host` — there is no `-d www.host`. The resulting certificate's SAN list does not cover www.<host>, so any visitor reaching https://www.<host> gets a TLS hostname-mismatch error and the page fails to load. Either the cert must include the www SAN, or www must not be advertised in the HTTPS server_name.
- **Fix:** Add `-d`, `www.${host}` to the LE issuance args so the cert SAN covers www, or drop `www.${host}` from the :443 server_name (keeping it on :80 to 301 www → https://<host>).
- **Verifier:** The reviewer's snippet matches the code verbatim. In src/lib/plesk/provision-domain.ts, renderTenantNginxConfig emits the :443 block with `server_name ${host} www.${host};` (line 97) and points TLS at a single apex-keyed cert: `ssl_certificate ${LE_CERT_DIR}/${host}/fullchain.pem;` (line 99) and privkey.pem (line 100). The cert is issued in issueLetsEncryptCert with args (lines 128-133): `['bin','extension','--exec','letsencrypt','cli.php','-d', host, '-m', ownerEmail(), '-w', ACME_WEBROOT]` — only `-d host`, no `-d www.${host}`. Plesk's letsencrypt cli.php builds the SAN list from exactly the

### 18. nav_header detector emits logo_url, but the Pages nav_header block has no logo_url field, so the logo is silently dropped on save
- **Where:** `src/lib/builder/html-to-structured-blocks.ts:101`  ·  **Subsystem:** Page builder lib  ·  **Category:** data-integrity  ·  **Reviewer confidence:** high
- **Problem:** detectNavHeader extracts the first <img> src into logo_url and returns it on the nav_header block. BlockRenderer.tsx reads it (line 261: `get<string>(block, 'logo_url') ?? (ctx.site).logo_url ?? ...`), so the renderer DOES honor a per-block logo override. But the nav_header block definition in src/collections/Pages.ts (lines 118-132) has no `logo_url` field (only links, cta_label, cta_href, show_phone). Because both the structured-fields import (html-import-action.ts -> payload.create with overrideAccess:false) and the in-editor convert action (convert-action.ts) persist the detected block through Payload's per-field validation, the unknown logo_url is silently stripped on save. The author sees the extracted brand logo in the live preview but loses it the moment the page is saved/reloaded -- a lock-step gap (field present in detector + renderer, missing in Pages.ts).
- **Fix:** Add `{ name: 'logo_url', type: 'text' }` to the nav_header block fields in src/collections/Pages.ts (and to NavHeaderSchema in block-schemas.ts for the AI path), then run pnpm generate:types. This keeps the detector, renderer, schema, and Pages collection in lock-step so the imported logo persists.
- **Verifier:** The bug is real and reachable. Verified the full chain:

1. DETECTOR emits logo_url. src/lib/builder/html-to-structured-blocks.ts line 80-82 pulls the first <img> src into logoUrl, and the returned nav_header block includes it: line 101 `logo_url: logoUrl || undefined`.

2. NORMALIZE does not strip it. In src/lib/builder/block-schemas.ts, normalizeAIBlocks() has no `nav_header` branch, so the block falls through to the default `out.push(b)` (line 335), preserving logo_url unchanged.

3. PERSISTENCE goes through Payload's blocks field. src/app/(app)/admin/sites/[slug]/pages/new/html-import-acti

### 19. AuditLog create access is `() => true`, allowing forged/spoofed audit entries via REST
- **Where:** `src/collections/AuditLog.ts:19`  ·  **Subsystem:** Access control & multi-tenancy  ·  **Category:** data-integrity  ·  **Reviewer confidence:** high
- **Problem:** create access is an unconditional `() => true` that ignores req.user entirely. The comment claims 'hook-driven, never via UI', but access rules gate the REST surface: POST /api/audit-log is governed only by this rule, so ANY caller (the function never checks authentication) can insert arbitrary audit rows. Because `user`, `action`, `entity_type`, and `entity_id` are caller-supplied, an attacker can fabricate audit history, attribute actions to other users, or spam the log — defeating the integrity guarantee of the audit trail (which is the compliance record of every mutation). The auditAfterChange/Delete hooks pass `req` with an authenticated user, so tightening this to isAuthenticated (or better, require !overrideAccess context) would not break the hooks.
- **Fix:** Restrict create to authenticated requests at minimum (`create: isAuthenticated`) so anonymous forging is blocked; ideally only allow the internal hook path (e.g. gate on a server-only flag) so end users can never write audit rows directly.
- **Verifier:** Evidence is accurate. src/collections/AuditLog.ts:19 reads `create: () => true, // hook-driven, never via UI` with `update: () => false` on line 20. This access function ignores `req.user` entirely and always returns true, so the auto-generated Payload REST endpoint `POST /api/audit-log` is gated only by this rule and accepts requests from any caller, authenticated or not. CORS is `'*'` (payload.config.ts:88); CSRF (payload.config.ts:94) only governs whether cookie auth is honored, not whether an anonymous (cookie-less) POST is permitted, so it does not block this. The collection has no `endpo

### 20. TrackingConfigs read access (siteScopedRead) lets analyst-role users read per-site server secrets
- **Where:** `src/collections/TrackingConfigs.ts:13-17`  ·  **Subsystem:** Collections schema correctness  ·  **Category:** security  ·  **Reviewer confidence:** low
- **Problem:** TrackingConfigs grants `read: siteScopedRead` while create/update/delete are `siteScopedAdmin`. `siteScopedRead` admits the `analyst` role (rank 1) per src/access/index.ts, so an analyst bound to a Site can read this row — which holds server-side credentials: `meta_pixel.capi_token` (explicitly documented "Stored encrypted; never sent to client."), `ga4.api_secret`, `tiktok.access_token`, `trustedform.api_key`, `truecall.api_key`, `jornaya` ids, and `custom_webhooks[].hmac_secret`. These are reachable via the REST surface (`/api/tracking-configs`) and the raw /cms admin regardless of the custom UI. This is inconsistent with the project's hard rule that integration credentials are server-side only, and with least privilege for the read-only analyst role. Read should likely be `siteScopedAdmin` to match the write rules, or the secret fields should carry field-level read access restricting them to admins/super-admins.
- **Fix:** Either set `read: siteScopedAdmin` for TrackingConfigs, or add field-level `access: { read: ... }` to the credential fields (capi_token, api_secret, access_token, api_key, hmac_secret) so the analyst role cannot read raw secrets.
- **Verifier:** Verified in src/collections/TrackingConfigs.ts lines 12-17: `read: siteScopedRead`, while `create/update/delete: siteScopedAdmin`. In src/access/index.ts, `siteScopedRead` (lines 26-33) calls `siteIdsForUser(user, 'analyst')`; the rank map is `{ analyst: 1, editor: 2, admin: 3 }` (line 19) with `min = rank['analyst'] = 1`, and the filter `rank[b.role] >= min` (line 22) admits analyst (rank 1). So an analyst-role user bound to a Site receives `{ site: { in: siteIds } }` and can READ that Site's TrackingConfig, whereas writes require admin (`siteScopedAdmin` → min rank 3, lines 44-51). The row h

### 21. saveGeneralSettings has no try/catch — access-denied / duplicate-slug / validation errors become unhandled rejections, client never sees {ok:false}
- **Where:** `src/app/(app)/admin/sites/[slug]/settings/general/actions.ts:56-67`  ·  **Subsystem:** Admin server actions (sites/users/settings)  ·  **Category:** error-handling  ·  **Reviewer confidence:** high
- **Problem:** The action's return type is `{ ok: boolean; error?: string }` and the client (GeneralForm.tsx onSubmit) branches on `result.ok` / sets `result.error`. But `payload.update` is called with NO try/catch. With `overrideAccess: false`, the Sites `update` access rule requires an `admin`-role binding for that site, so an `editor`/`analyst` (or a user bound to a different site) triggers an Access-denied throw. The `slug` field is freely editable and is `unique: true` on Sites, so renaming to an existing slug throws a DB unique-constraint error. Any required-field validation error throws too. In every case the server action rejects, the `await saveGeneralSettings(formData)` inside the `useTransition` callback throws, the `else setError(...)` branch never runs, and the user gets no feedback (or an unhandled error overlay) instead of a clean error message. Compare to saveTracking/updateProfile/inviteUser in this same subsystem, which all wrap the write in try/catch.
- **Fix:** Wrap the payload.update in try/catch and return `{ ok: false, error: e instanceof Error ? e.message : 'Failed to save settings' }` on failure, matching the pattern in saveTracking/updateProfile.
- **Verifier:** The claim is accurate. In src/app/(app)/admin/sites/[slug]/settings/general/actions.ts the action saveGeneralSettings returns Promise<{ ok: boolean; error?: string }> (line 16) but lines 56-66 call `const payload = await getPayload({ config }); await payload.update({ collection: 'sites', id: siteId, data, user: user as never, overrideAccess: false })` with NO try/catch — only the pre-write guards (lines 18, 21) return {ok:false}. Any throw from payload.update rejects the server action.

The throw paths are real and reachable: (1) Access — src/collections/Sites.ts update access (lines 23-30) re

### 22. saveGeneralSettings revalidates the NEW slug path and never redirects — a slug rename strands the user on a now-dead URL
- **Where:** `src/app/(app)/admin/sites/[slug]/settings/general/actions.ts:65`  ·  **Subsystem:** Admin server actions (sites/users/settings)  ·  **Category:** routing  ·  **Reviewer confidence:** high
- **Problem:** The route is `/admin/sites/[slug]/settings/general` and the page resolves the site by `where: { slug: { equals: slug } }` from the URL param. When the operator edits the `slug` field, the action updates the Site to the new slug but only calls `revalidatePath` for the NEW slug path (`data.slug`) — a path the browser is not on — and returns `{ ok: true }` with no redirect. The client stays on `/admin/sites/<old-slug>/settings/general`; any refresh or navigation to that URL now fails to resolve the Site (its slug changed) and 404s, while the just-revalidated new-slug path is never visited. The stale old-slug cache entry is also never invalidated.
- **Fix:** If the slug changed, revalidate the old path too and return the new slug so the client can `router.replace(\`/admin/sites/${newSlug}/settings/general\`)`; otherwise disallow slug edits in this form.
- **Verifier:** Verified across all three files. page.tsx (lines 13-20) resolves the Site strictly by the URL param: `where: { slug: { equals: slug } }` ... `if (!site) notFound()`. So any refresh of `/admin/sites/<old-slug>/settings/general` after a slug change resolves zero docs and 404s. actions.ts (lines 57-66) updates the Site then runs `revalidatePath(`/admin/sites/${data.slug}/settings/general`)` using the NEW slug — a path the browser is not on — and `return { ok: true }` with no redirect and no return of the new slug. GeneralForm.tsx has the slug as a plain editable input (line 63: `<input name="slug

### 23. createSite only checks authentication, not super_admin — non-super-admins hit an unguarded payload.create that throws an unhandled rejection
- **Where:** `src/app/(app)/admin/(top)/sites/actions.ts:82-204`  ·  **Subsystem:** Admin server actions (sites/users/settings)  ·  **Category:** error-handling  ·  **Reviewer confidence:** high
- **Problem:** createSite guards only with `if (!user) return ...`. It then does the slug-conflict find and finally `payload.create({ collection: 'sites', overrideAccess: false })`. The Sites `create` access rule is `isSuperAdmin`, so any authenticated NON-super-admin (editor/analyst, or an admin-of-a-site) passes the `!user` check, proceeds through the LLM/branch logic, and then the create throws Access-denied. That create is not wrapped in try/catch, so the server action rejects and CreateSiteWizard's `result.ok` branch never runs (the await throws inside the transition) — no clean error. The sibling user/integration actions in this subsystem gate with an explicit `requireSuperAdmin()` helper; this one does not.
- **Fix:** Add an explicit `if (!user.super_admin) return { ok: false, error: 'forbidden — super admin required' }` early-return, and/or wrap the create chain in try/catch returning `{ ok: false, error }`.
- **Verifier:** The bug is real and reachable. createSite (src/app/(app)/admin/(top)/sites/actions.ts:82-84) guards only with `const user = await getCurrentUser(); if (!user) return { ok: false, error: 'unauthenticated' }` — no super_admin check. It then calls `payload.create({ collection: 'sites', data: siteData as never, user: user as never, overrideAccess: false })` at lines 199-204 with NO surrounding try/catch (the only try/catch in the function wraps the LLM call at 160-195 and the optional quiz/LP creates at 258-297; the Sites create itself is bare).

The Sites `create` access rule is `create: isSuperA

### 24. createSite leaves a partially-built (orphaned) Site if any post-create step throws — no rollback or try/catch around preview-domain, pages, or tracking creation
- **Where:** `src/app/(app)/admin/(top)/sites/actions.ts:207-252`  ·  **Subsystem:** Admin server actions (sites/users/settings)  ·  **Category:** data-integrity  ·  **Reviewer confidence:** medium
- **Problem:** After the Site row is created (step 1), createSite sequentially creates the preview Domain (step 2), loops to create default/cloned Pages (step 3), and creates the TrackingConfig singleton (step 4) — none of these are wrapped in try/catch (only the later quiz/landing-page creates are). If the preview-domain create, any Pages-loop iteration, or the tracking-config create throws (e.g. transient DB error, a body_blocks validation failure on a cloned page), the action rejects mid-way leaving a committed Site (and possibly a partial set of domains/pages) with no rollback. The result is an orphaned half-provisioned Site that the operator cannot finish through the wizard (slug now conflicts on retry) and that has no primary domain or incomplete legal pages.
- **Fix:** Wrap the post-create steps in try/catch and, on failure, delete the just-created Site (and its children) before returning `{ ok: false, error }`, so a failed creation does not leave an unusable half-site.
- **Verifier:** Verified against src/app/(app)/admin/(top)/sites/actions.ts. The Site is committed at lines 199-204 (`payload.create({ collection: 'sites', ... })`). The subsequent steps are NOT wrapped in try/catch: step 2 preview-domain create (lines 209-219), step 3 pages loop (lines 229-245: `for (const page of pages) { await payload.create({ collection: 'pages', ... }) }`), and step 4 tracking-config create (lines 248-252). By contrast, step 5 (quiz at 258-276 and landing page at 278-297) IS individually wrapped in try/catch that only console.warns, confirming the reviewer's noted inconsistency. The func

### 25. listSiteMedia leaks another tenant's media (cross-tenant read via overrideAccess: true)
- **Where:** `src/app/(app)/admin/sites/[slug]/pages/[id]/media-actions.ts:74-101`  ·  **Subsystem:** Page-builder server actions  ·  **Category:** tenant-isolation  ·  **Reviewer confidence:** high
- **Problem:** listSiteMedia is a server action whose only guard is getCurrentUser() (any authenticated user). It takes siteId straight from the client and runs payload.find on the media collection with overrideAccess: true, which completely bypasses the collection's read access (siteScopedRead). Server actions are independently-invocable POST endpoints — the [slug]/layout.tsx isBoundToSite check does NOT run for them. So an analyst/editor bound only to Site A can call listSiteMedia({ siteId: <Site B id> }) and receive Site B's media URLs, filenames and alt text. The repo already has isBoundToSite() in src/lib/auth.ts for exactly this, but it is not called here. The companion uploadMediaFromDataURL is safe because its create uses overrideAccess: false; only the read path is unguarded.
- **Fix:** Verify the caller is bound to args.siteId before reading: import { isBoundToSite } from '@/lib/auth' and `if (!isBoundToSite(user, args.siteId)) return { ok: false, error: 'forbidden' }`. Alternatively drop overrideAccess (use overrideAccess: false and pass `user`/`req`) so siteScopedRead filters the result to the user's sites.
- **Verifier:** Confirmed real cross-tenant read. In src/app/(app)/admin/sites/[slug]/pages/[id]/media-actions.ts the `listSiteMedia` action (lines 74-101) guards only with `getCurrentUser()` (lines 80-81 — any authenticated user), takes `args.siteId` straight from the client (line 75), and runs `payload.find({ collection: 'media', where: { site: { equals: args.siteId } }, ..., overrideAccess: true })` (lines 85-91). The reviewer's evidence snippet is verbatim-accurate. The `media` collection sets `read: siteScopedRead` (src/collections/Media.ts), whose comment states "an admin for site A can't see site B's u

### 26. AI clone burns an LLM call before verifying the user may write to the target site
- **Where:** `src/app/(app)/admin/sites/[slug]/pages/new/ai-clone-action.ts:42-50`  ·  **Subsystem:** Page-builder server actions  ·  **Category:** tenant-isolation  ·  **Reviewer confidence:** medium
- **Problem:** createPageFromUrl only checks getCurrentUser() up front; the actual access enforcement (overrideAccess: false on payload.create) happens at line 162, AFTER the external fetch and the full invokeLLM call (maxTokens 8192). A user not bound to args.siteId (or bound to a different site) can therefore drive arbitrary paid LLM clone calls and external fetches that always ultimately fail at create. This is a cost/abuse and SSRF-amplification gap stemming from the missing binding check; the same pattern (no isBoundToSite on a client-supplied siteId) affects createPage, createPageFromHtml, savePageBodyBlocks and uploadMediaFromDataURL, but those at least do no expensive work before the protected write.
- **Fix:** Add `if (!isBoundToSite(user, args.siteId)) return { ok: false, error: 'forbidden' }` (from @/lib/auth) immediately after the getCurrentUser() check, before the fetch and invokeLLM call.
- **Verifier:** The claim is accurate and reachable. In src/app/(app)/admin/sites/[slug]/pages/new/ai-clone-action.ts, createPageFromUrl(args) only checks authentication: lines 49-50 `const user = await getCurrentUser(); if (!user) return { ok: false, error: 'unauthenticated' }`. args.siteId is a client-supplied parameter (line 43-48). The action then performs an external fetch loop over two user-agents (lines 70-87, an SSRF surface) and an invokeLLM call with maxTokens: 8192 (lines 112-145) UNCONDITIONALLY for any authenticated user. The only access enforcement is at lines 162-177: payload.create({ collectio

### 27. numFromBrandId maps empty/no-brand selection to site id 0 instead of null
- **Where:** `src/app/(app)/admin/(top)/quizzes/actions.ts:14-18,124`  ·  **Subsystem:** Funnel builder (authoring + deployment)  ·  **Category:** data-integrity  ·  **Reviewer confidence:** high
- **Problem:** `numFromBrandId('')` returns 0, not null: typeof '' === 'string' is true, ''.replace('site_','') === '', Number('') === 0, and Number.isFinite(0) === true. The quiz Deployment editor's Brand <Select> includes an empty `<option value="">- pick brand -</option>` (QuizBuilderApp.tsx:258) and handleSave performs no brand validation, so a user can save a deployment with brandId === ''. That empty value flows into `site: numFromBrandId(dep.brandId)` and is written as `site: 0` rather than `null`. site id 0 does not exist, so Payload either rejects the relationship (save fails) or persists a dangling FK to a nonexistent Site. The same defect exists for the literal value 'site_' (Number('') === 0). The intent is clearly null when no brand is selected. The identical helper/bug is present in advertorials/actions.ts (saveAdvertorialDeployment, site: numFromBrandId(dep.brandId)).
- **Fix:** Treat an empty/zero numeric id as null, e.g.: `const numFromBrandId = (brandId) => { if (typeof brandId !== 'string') return null; const m = brandId.match(/^site_(\d+)$/); return m ? Number(m[1]) : null }`. This returns null for '', 'site_', and any non-`site_<digits>` value, and avoids ever writing site: 0.
- **Verifier:** Verified the helper at src/app/(app)/admin/(top)/quizzes/actions.ts:14-18 matches the reviewer's snippet verbatim: `const n = Number(brandId.replace('site_', '')); return Number.isFinite(n) ? n : null`. Ran the exact function: numFromBrandId('') === 0 and numFromBrandId('site_') === 0 (because ''.replace('site_','')==='' , Number('')===0, Number.isFinite(0)===true), while numFromBrandId('abc')===null. The reviewer's JS reasoning is correct.

Reachability confirmed end-to-end: brand ids are `site_<n>` strings (src/lib/brand-map.ts:18,61 `id: \`site_${id}\``). QuizBuilderApp.tsx:258 renders `<Se

### 28. saveQuizDeployment writes site 0 for unselected brand (same numFromBrandId defect)
- **Where:** `src/app/(app)/admin/(top)/advertorials/actions.ts:14-18,92`  ·  **Subsystem:** Funnel builder (authoring + deployment)  ·  **Category:** data-integrity  ·  **Reviewer confidence:** high
- **Problem:** Same root cause as the quizzes action: numFromBrandId returns 0 for an empty or 'site_' brandId, so saveAdvertorialDeployment persists `site: 0` instead of `null` when no brand is bound. Note the advertorial relationship field IS properly guarded (`dep.advertorialId && /^\d+$/.test(...) ? Number(...) : null`) and the quiz deployment's quiz id uses `dep.quizId ? Number(dep.quizId) : null` — but the `site` field uniquely lacks the empty-string guard, so it is the one that silently produces site id 0 for an unbound deployment.
- **Fix:** Fix numFromBrandId as described in the quizzes finding (anchor on /^site_(\d+)$/), or short-circuit: `site: dep.brandId ? numFromBrandId(dep.brandId) : null` plus an inner check that rejects 0/NaN.
- **Verifier:** The defect is real and reachable. In src/app/(app)/admin/(top)/advertorials/actions.ts:14-18, `numFromBrandId` does `if (typeof brandId !== 'string') return null; const n = Number(brandId.replace('site_', '')); return Number.isFinite(n) ? n : null`. For an empty string or 'site_' the replace yields '' and `Number('')` is `0`, which is finite, so it returns `0` (verified at runtime: `numFromBrandId('')` -> 0, `numFromBrandId('site_')` -> 0). Line 91 uses `site: numFromBrandId(dep.brandId)`, while every sibling relationship in the same data object maps empty -> null: line 90 `advertorial: dep.ad

### 29. Client Meta Pixel fires 'Submit form event' but server CAPI fires 'Lead' — event_id dedupe never matches (double-counted conversions)
- **Where:** `src/components/public/check-my-claim/Submitted.tsx:30`  ·  **Subsystem:** Public check-my-claim / quiz / survey flow  ·  **Category:** compliance  ·  **Reviewer confidence:** high
- **Problem:** The HARD RULE is that Pixel + CAPI conversions share one event_id per the Meta dedupe contract. Meta deduplicates a browser Pixel event against a Conversions API event ONLY when BOTH the event_id AND the event_name match. Here the client fires fbq('track', 'Submit form event', ..., {eventID: eventId}) while the server-side CAPI fires event_name: 'Lead' (src/lib/lead-pipeline/run.ts:204) with the same event_id. Because the names differ ('Submit form event' vs 'Lead'), Meta will treat them as two distinct conversions and count both — defeating the entire point of sharing event_id and inflating reported Lead conversions ~2x. Additionally 'Submit form event' (with spaces) is not a Meta standard event, so fbq('track', ...) registers it as an unrecognized/custom-named event rather than the standard 'Lead'.
- **Fix:** Fire the same standard event name the CAPI side uses: window.fbq?.('track', 'Lead', {}, eventId ? { eventID: eventId } : undefined). Keep the event_id identical (already passed) so Pixel/CAPI deduplicate correctly.
- **Verifier:** Verified the reviewer's evidence verbatim. src/components/public/check-my-claim/Submitted.tsx:30 fires `window.fbq?.('track', 'Submit form event', {}, eventId ? { eventID: eventId } : undefined)`. The server CAPI side at src/lib/lead-pipeline/run.ts:204 sends `event_name: 'Lead'` with the same shared `event_id` (line 206). Meta deduplicates a browser Pixel event against a CAPI event only when BOTH event_id AND event_name match; 'Submit form event' != 'Lead', so the shared event_id is wasted and the conversion is double-counted. The codebase's own canonical lead form proves the intended/correct

### 30. TikTok client Pixel omits event_id and uses a different event name than the TikTok Events API call — no deduplication
- **Where:** `src/components/public/check-my-claim/Submitted.tsx:33`  ·  **Subsystem:** Public check-my-claim / quiz / survey flow  ·  **Category:** compliance  ·  **Reviewer confidence:** high
- **Problem:** The server-side TikTok Events API fires event: 'SubmitForm' WITH event_id (src/lib/lead-pipeline/run.ts:252-254) so the browser pixel and server event can deduplicate. The client here fires ttq.track('Submit form event', {}) — a different event name ('Submit form event' vs 'SubmitForm') AND passes no event_id at all. TikTok dedupes browser vs server events on matching event name + event_id; with neither matching, the conversion is double-counted (once browser, once server).
- **Fix:** Match the CAPI event and pass the shared id: window.ttq?.track?.('SubmitForm', {}, eventId ? { event_id: eventId } : undefined). Use the same name 'SubmitForm' the Events API call uses and forward the event_id from the query string.
- **Verifier:** Both halves of the claim are verbatim-accurate in the current code.

CLIENT (src/components/public/check-my-claim/Submitted.tsx:33): `window.ttq?.track?.('Submit form event', {})` — fires event name 'Submit form event' and passes NO third argument, so no event_id. The adjacent Meta call on line 30 (`window.fbq?.('track', 'Submit form event', {}, eventId ? { eventID: eventId } : undefined)`) shows the codebase knows how to forward the shared id (`const eventId = search.get('event_id')` on line 25), making the TikTok omission a genuine oversight rather than an intentional design.

SERVER (src/li

### 31. refreshSystemReport server action only checks login, not super_admin
- **Where:** `src/app/(app)/admin/(top)/system/actions.ts:7-12`  ·  **Subsystem:** System health / self-check / media / misc API  ·  **Category:** security  ·  **Reviewer confidence:** high
- **Problem:** The refreshSystemReport server action gates on `if (!user) return` but performs no super_admin check. Any authenticated user (including a single-tenant analyst/editor) can invoke it to force-invalidate the cached report, which makes the next render of the system page re-run all checks — outbound Cloudflare DoH lookups, a Plesk REST call, a TLS handshake to the fallback host, and several DB count queries. Combined with the unguarded system page, this lets a non-privileged user both view the infra report and force its (somewhat expensive, network-fanout) regeneration. Same gap exists in settings/system/actions.ts.
- **Fix:** Add `if (!user.super_admin) return` after the existing null check, matching the access level the page itself should enforce.
- **Verifier:** The reviewer's snippet is verbatim-accurate in BOTH files. src/app/(app)/admin/(top)/system/actions.ts:7-12 and the duplicate at .../settings/system/actions.ts:7-12 read: `export async function refreshSystemReport(): Promise<void> { const user = await getCurrentUser(); if (!user) return; invalidateSystemReport(); revalidatePath('/admin/...') }` — only a login check, no super_admin gate.

No upstream gate compensates: the root admin layout src/app/(app)/admin/layout.tsx:18-19 only does `if (!user) redirect('/sign-in...')`; the (top) layout does no role check; middleware.ts:5,15 passes all of `/

### 32. Media upload accepts unsanitized SVG, served same-origin (stored XSS)
- **Where:** `src/app/api/media/upload/route.ts:14-26`  ·  **Subsystem:** System health / self-check / media / misc API  ·  **Category:** security  ·  **Reviewer confidence:** medium
- **Problem:** The upload route accepts image/svg+xml, writes the raw bytes to public/uploads/<random>.svg, and returns `/uploads/<file>.svg`. Files in public/ are served as static assets on the same origin as the admin (os.legenex.com) with Content-Type image/svg+xml. An SVG can carry <script> or on* event handlers, so navigating directly to the returned URL executes attacker-controlled JavaScript in the admin's origin — a stored-XSS vector. Upload requires authentication, but an `editor` on a single tenant can upload, and the resulting URL is same-origin with the privileged admin/CMS, so the blast radius crosses tenant/privilege boundaries. No sanitization or Content-Disposition/CSP mitigation is applied.
- **Fix:** Either drop image/svg+xml from ALLOWED_MIME, or sanitize SVG bytes (e.g. DOMPurify with SVG profile / svgo strip-scripts) before writing, and serve uploads with `Content-Security-Policy: default-src 'none'` / `Content-Disposition: attachment` so the markup can't execute in the admin origin.
- **Verifier:** The bug is real and reachable. Verified the full chain in src/app/api/media/upload/route.ts:

1. Auth is the only gate, and it's permissive. route.ts:29-32 only does `const user = await getCurrentUser(); if (!user) return 401`. getCurrentUser (src/lib/auth.ts:13-18) just returns `payload.auth({headers}).user` — no role check, no super_admin check, no site scoping. Any authenticated user, including a single-tenant `editor`, passes.

2. SVG is accepted: ALLOWED_MIME contains `'image/svg+xml'` (route.ts:15) and EXT_BY_MIME maps it to `.svg` (route.ts:23). file.type is checked against ALLOWED_MIME

### 33. 20260529_060000 comment falsely claims brand_identity has existed since init, masking the missing-column bug
- **Where:** `src/migrations/20260529_060000_sites_global_blocks.ts:6-11`  ·  **Subsystem:** Migrations vs schema drift  ·  **Category:** schema-migration  ·  **Reviewer confidence:** high
- **Problem:** The migration header states the global-nav/global-footer values 'are now stored inside the existing `brand_identity` jsonb column instead, which has been on the table since the initial migration — no schema change needed.' This is factually wrong: the init migration never creates a brand_identity column (verified against both 20260513_221103_init.ts and its JSON snapshot). Anyone debugging the global-blocks feature will trust this comment, conclude brand_identity exists, and miss that the column is absent in production. The no-op migration therefore leaves the global nav/footer feature relying on a column that does not exist on a migrations-only prod DB.
- **Fix:** Either add brand_identity (and the rest of the missing Sites columns) in a real migration per the prior finding, then correct this comment; or, if this no-op truly depends on brand_identity, have THIS migration ADD COLUMN IF NOT EXISTS "brand_identity" jsonb so the feature it documents actually has its backing column.
- **Verifier:** The claim is verified accurate, not refutable.

1) The comment is factually false. `src/migrations/20260529_060000_sites_global_blocks.ts` lines 9-10 state the values "are now stored inside the existing `brand_identity` jsonb column instead, which has been on the table since the initial migration — no schema change needed." I read the init migration's full `CREATE TABLE "sites"` block (`src/migrations/20260513_221103_init.ts` lines 66-95): it defines `brand_logo_url`, `brand_favicon_url`, `brand_primary`...`brand_font_body`, `default_tone`, `default_disclaimer_md`, `archived_at`, etc., but the


## LOW (16)

### 34. Enabled-but-misconfigured TikTok/GA4/TrueCall integrations report ok:true and skip the delivery log
- **Where:** `src/lib/lead-pipeline/run.ts:235-236`  ·  **Subsystem:** Lead pipeline core  ·  **Category:** error-handling  ·  **Reviewer confidence:** high
- **Problem:** When an integration is enabled but missing credentials, Meta CAPI correctly records `ok: false` and calls `logDelivery(..., false, 'missing credentials')` (run.ts lines 194-196). TikTok, GA4, and TrueCall instead collapse the enabled-but-missing-creds case into the same branch as the disabled case and push `ok: true` with no `logDelivery` call. The detail string distinguishes them ('missing credentials' vs 'skipped'), but the step is marked successful and nothing is written to the lead's delivery_log, so an operator who turned the integration on but fat-fingered a credential gets no failure signal in the per-lead delivery trace and the pipeline 200s as if everything fired.
- **Fix:** Split the disabled vs. misconfigured cases like the Meta task does: when `tk.enabled` is true but a credential is missing, push `ok: false` and call `logDelivery(step, false, 'missing credentials')`. Apply the same fix to the ga4Task (line 281) and truecallTask (line 318) branches.
- **Verifier:** Read src/lib/lead-pipeline/run.ts directly. The claim accurately reflects the code. Reference behavior — Meta CAPI (lines 190-198) and TrustedForm (lines 140-148) — splits two cases: disabled → push {ok: true, detail: 'skipped'} with no logDelivery; enabled-but-missing-creds → push {ok: false, detail: 'missing credentials'} AND call logDelivery(step, false, 'missing credentials').

TikTok collapses both into one branch (lines 234-237):
  if (!tk?.enabled || !tk.pixel_code || !tk.access_token) {
    steps.push({ step: 'tiktok.events_api', ok: true, detail: tk?.enabled ? 'missing credentials' : 

### 35. HLR carrier.type is cast from Plivo's raw value without mapping — Plivo "fixed" never becomes "landline"
- **Where:** `src/lib/integrations/hlr.ts:53`  ·  **Subsystem:** External integrations  ·  **Category:** data-integrity  ·  **Reviewer confidence:** medium
- **Problem:** The HLRResult declares `carrier.type` as the union `'mobile' | 'landline' | 'voip' | 'unknown'`, but the Plivo lookup blind-casts the provider's raw `r.carrier?.type` string into that union with no value mapping. Plivo's carrier lookup returns types such as `"fixed"` (for landlines), `"mobile"`, and `"voip"`. A `"fixed"` line is therefore persisted into the lead's `hlr_result` JSON as `type: "fixed"`, which never matches the documented `'landline'` value any downstream consumer would check for. The stored data silently misrepresents the carrier classification.
- **Fix:** Map Plivo's vocabulary onto the result union explicitly, e.g. `const raw = r.carrier?.type; const type = raw === 'fixed' ? 'landline' : (raw === 'mobile' || raw === 'voip' ? raw : 'unknown')` before assigning, rather than casting the provider string straight through.
- **Verifier:** The cited code at src/lib/integrations/hlr.ts:53 matches the evidence verbatim: `type: (r.carrier?.type as 'mobile' | 'landline' | 'voip' | 'unknown') ?? 'unknown',`. The `as` is a TypeScript compile-time assertion only — it performs no runtime value transformation, so whatever string Plivo returns is stored verbatim. The source field is typed `string` at line 39 (`carrier?: { name?: string; type?: string; ... }`), and the result is persisted whole as opaque JSON into the lead via run.ts:459-464 (`data: { hlr_result: res as never }`).

I verified the factual premise: Plivo's Lookup carrier API

### 36. redirects_from[].host is never normalized, so case/whitespace variants silently fail to 301
- **Where:** `src/collections/Domains.ts:37-56`  ·  **Subsystem:** Site resolution & public routing  ·  **Category:** routing  ·  **Reviewer confidence:** high
- **Problem:** The Domains beforeValidate hook normalizes only the top-level `data.host` (lowercase/trim/strip protocol/strip trailing slash) but leaves the `redirects_from[].host` sub-field exactly as typed. The resolver queries with the normalized incoming host: `where: { 'redirects_from.host': { equals: host } }` (site-resolver.ts:63) where `host` has been lowercased by `normalizeHost`. So if an admin enters `OldBrand.com` (any uppercase) or a value with a trailing space/slash into redirects_from, the equality match against the lowercased incoming host never succeeds and the old-domain 301 silently no-ops, dropping traffic from the migrated domain. (The primary `host` column works because it is normalized; only the redirect aliases are affected.)
- **Fix:** In the same beforeValidate hook, also normalize each entry: `if (Array.isArray(data.redirects_from)) data.redirects_from = data.redirects_from.map(r => ({ ...r, host: normalizeHost(r.host) }))` using the same normalization (or import normalizeHost from site-resolver).
- **Verifier:** Confirmed and reachable. In src/collections/Domains.ts the beforeValidate hook (lines 38-56) normalizes ONLY the top-level host: line 40 `const normalized = data.host.toLowerCase().trim().replace(/^https?:\/\//, '').replace(/\/$/, '')` and line 55 `return { ...data, host: normalized }` — `redirects_from` is carried through unchanged via the `...data` spread. The redirects_from field (lines 131-135) is a plain text array (`fields: [{ name: 'host', type: 'text', required: true }]`) with no field-level hook. A repo-wide grep for `redirects_from` shows the only references are the field def, the re

### 37. LandingPage and BlogPost bodies skip {{site.*}} substitution, so raw placeholders can leak to public visitors
- **Where:** `src/app/(public)/[[...slug]]/page.tsx:507-535`  ·  **Subsystem:** Site resolution & public routing  ·  **Category:** correctness  ·  **Reviewer confidence:** medium
- **Problem:** RenderPage runs deepRenderTemplateVars over its blocks (line 384), but RenderLandingPage renders lp.hero/body_sections and BlogPost body (lines 303-310) directly without any renderTemplateVars/deepRenderTemplateVars pass. The hard rule in template-vars.ts states 'public pages must NEVER show raw {{placeholders}} to end users.' If an author types `{{site.phone}}` or `{{site.name}}` into a LandingPage hero/section or a blog body (the same variables that work on Pages), it renders literally as `{{site.phone}}` to the visitor instead of the resolved value.
- **Fix:** Run renderTemplateVars/deepRenderTemplateVars on lp.hero and body_sections (and the blog post title/body) with the resolved site before rendering, matching the Pages path.
- **Verifier:** The claim is accurate and reachable. In src/app/(public)/[[...slug]]/page.tsx, RenderLandingPage (lines 507-535) renders lp.hero fields and body_sections directly into JSX with no substitution: line 519 `{lp.hero?.eyebrow ...}`, line 520 `<h1>{lp.hero?.heading ?? lp.name}</h1>`, line 521 `{lp.hero.sub}`, line 524 `<h2>{s.heading}</h2>`, line 525 `<pre ...>{s.body_markdown}</pre>`. The BlogPost render (lines 303-310) likewise outputs `{post.docs[0].title}` and `{post.docs[0].body_markdown}` raw. Neither path calls renderTemplateVars/deepRenderTemplateVars. Contrast with RenderPage line 384 `con

### 38. attachDomainToSite swallows a failed verify and returns ok:true
- **Where:** `src/app/(app)/admin/(top)/brands/domains/actions.ts:92-94`  ·  **Subsystem:** Domain provisioning / Plesk / SSL / DNS  ·  **Category:** error-handling  ·  **Reviewer confidence:** medium
- **Problem:** After attaching the domain, attachDomainToSite calls verifyAndPromoteDomain and, when that returns { ok: false, error }, discards the error and returns { ok: true, verified: false } to the caller. The UI cannot distinguish 'DNS not ready yet' from a genuine failure (e.g. domain not found, unassigned, auth). The real error string is lost and never surfaced to the operator.
- **Fix:** Propagate the failure, e.g. `if (!result.ok) return { ok: false, error: result.error }`, so the Brands UI can show why verification could not run; leave the verified:false path for the legitimate 'DNS not ready' case where result.ok is true.
- **Verifier:** Verified against both files. In src/app/(app)/admin/(top)/brands/domains/actions.ts lines 92-94, attachDomainToSite does: `const result = await verifyAndPromoteDomain(...)` then `if (!result.ok) return { ok: true, verified: false }` then `return { ok: true, verified: 'verified' in result ? result.verified : false }`. The error string from result.error is discarded.

The key fact that makes this a genuine bug (not a false positive): in src/app/(app)/admin/sites/[slug]/settings/domains/actions.ts, the VerifyResult union (lines 14-28) and the function body show that the legitimate "DNS not ready 

### 39. Banned-vocab/em-dash check inspects the serialized JSON envelope (keys + structure), not text values
- **Where:** `src/lib/ai/invoke.ts:81-89`  ·  **Subsystem:** AI invoke layer  ·  **Category:** correctness  ·  **Reviewer confidence:** medium
- **Problem:** When enforceNoBannedVocab is set, the post-check runs hasBannedSignals(JSON.stringify(parsed.data)). It scans the whole serialized object including the field NAMES and JSON punctuation, not just the model's prose. The boundary logic in findBannedVocab treats non-alphanumeric chars (including '"', '_', ':') as word boundaries, so a schema key that is or contains a banned token will match. I verified this empirically: {essential:"clean value"} flags 'essential', and {navigate_to:"https://x.com"} flags 'navigate' (the '_' after 'navigate' passes the boundary test). Because the offending text is a fixed key name the model cannot change, every retry would re-fail and invokeLLM would throw after maxRetries+1 attempts despite clean content — silently breaking an AI feature. No currently-enforced schema (LP copy, block-schemas, humanizer, advertorial 'result'/'texts') uses a banned word as a key, so this is latent today, but adding a field named e.g. 'essential', 'vital', 'robust', 'navigate_*' to any enforced schema would brick that path. The em-dash count is likewise taken over JSON.stringify length, so structural characters inflate the per-text em-dash allowance.
- **Fix:** Run the check over the concatenated string *values* of parsed.data rather than the JSON envelope — e.g. recursively collect all string leaf values (JSON.stringify with a replacer that joins strings, or a small walker) and pass only that text to hasBannedSignals. This keeps the HARD-RULE guarantee on model-authored copy without false-positiving on field names / JSON structure.
- **Verifier:** The mechanism is real and I verified every load-bearing claim by reading the code.

src/lib/ai/invoke.ts:81-89 passes the serialized envelope to the checker, not the model's prose:
```
if (enforceNoBannedVocab) {
  const flat = JSON.stringify(parsed.data)
  const check = hasBannedSignals(flat)
```
`parsed.data` is the raw Zod parse output (line 74) with its full object structure; nothing extracts only string values before `JSON.stringify`. So keys and JSON punctuation are scanned.

src/lib/ai/banned-vocab.ts:44-61 `findBannedVocab` uses `wordBefore = /[a-z0-9]/i.test(before)` and `wordAfter = 

### 40. CSS :root variable parser drops the final declaration when it omits a trailing semicolon
- **Where:** `src/lib/builder/extract-brand-tokens.ts:9`  ·  **Subsystem:** Page builder lib  ·  **Category:** correctness  ·  **Reviewer confidence:** medium
- **Problem:** CSS_VAR_DECL = /--([a-zA-Z0-9-]+)\s*:\s*([^;]+);/g requires a literal trailing ';'. The last declaration inside a :root block frequently has no trailing semicolon (valid CSS, e.g. `:root{--primary:#0a3;--accent:#f80}`). parseRootVars runs this regex over the :root body, so the final var (`--accent` here) is never captured. extractBrandFromCss therefore misses the last brand token in any hand-written/minified CSS that omits the final semicolon, which can drop --site-primary/--site-accent from the brand-override block emitted during structured import. Reachable from extractStructuredFromHtml -> extractBrandFromCss -> parseRootVars.
- **Fix:** Make the trailing terminator optional / also accept end-of-body: e.g. `/--([a-zA-Z0-9-]+)\s*:\s*([^;]+)(?:;|$)/g` (the body has no closing brace since it is already the inner :root text), or split the body on ';' and parse each segment. Either way the final unterminated declaration is captured.
- **Verifier:** The claim is accurate and the bug is real and reachable.

src/lib/builder/extract-brand-tokens.ts:9: `const CSS_VAR_DECL = /--([a-zA-Z0-9-]+)\s*:\s*([^;]+);/g` — the regex requires a LITERAL trailing `;`. The value class `[^;]+` excludes only `;` (it includes `}`), so when the regex runs over a `:root` body, the final declaration with no trailing semicolon never matches.

src/lib/builder/extract-brand-tokens.ts:8 `ROOT_BLOCK = /:root\s*\{([\s\S]*?)\}/g` captures group 1 as the inner text between `{` and `}`, excluding the braces. So parseRootVars (lines 15-27) runs CSS_VAR_DECL over a body lik

### 41. Empty-string success_slug reloads the current page instead of redirecting
- **Where:** `src/components/blocks/LeadForm.tsx:185-186`  ·  **Subsystem:** Block renderer & lead form  ·  **Category:** routing  ·  **Reviewer confidence:** medium
- **Problem:** After a successful submit the form does `const slug = block.success_slug ?? '/submitted'; window.location.assign(slug)`. The `??` only falls back on null/undefined, not on an empty string. If an editor clears the success_slug field (it is a free-text field in Pages.ts with default '/submitted', but can be emptied), `success_slug` becomes '' and `window.location.assign('')` reloads the current URL rather than navigating to the thank-you page — the user is silently left on the form after a successful lead.
- **Fix:** Treat empty as missing: `const slug = block.success_slug || '/submitted'`.
- **Verifier:** Verified by reading the actual code. src/components/blocks/LeadForm.tsx:185-186 reads `const slug = block.success_slug ?? '/submitted'` then `window.location.assign(slug)`. The nullish coalescing `??` only falls back on null/undefined, not on an empty string, so an empty `success_slug` passes through to `window.location.assign('')`, which navigates to the current URL (reloads the form) rather than the thank-you page. The block field is declared optional string at LeadForm.tsx:24 (`success_slug?: string`). The field is reachable as an empty string: src/collections/Pages.ts:371 defines `{ name: 

### 42. SharedLegalTemplates has no afterDelete audit hook, so template deletions are unaudited
- **Where:** `src/collections/SharedLegalTemplates.ts:33-35`  ·  **Subsystem:** Access control & multi-tenancy  ·  **Category:** compliance  ·  **Reviewer confidence:** high
- **Problem:** SharedLegalTemplates allows delete (delete: isSuperAdmin) and attaches afterChange: [auditAfterChange] but NOT afterDelete: [auditAfterDelete]. Every other auditable collection (Users, Sites, Leads, Numbers, Domains, Media, Pages, LandingPages, BlogPosts, Quizzes, TrackingConfigs, all Funnel collections) attaches both. Deleting a shared legal template — a globally-shared compliance asset that affects every Site using it — therefore produces no audit-log entry, leaving a gap in the mutation trail for exactly the kind of change CLAUDE.md says must surface an affected-Sites list.
- **Fix:** Import auditAfterDelete and add `afterDelete: [auditAfterDelete]` to the hooks block, matching every other collection.
- **Verifier:** Verified in /workspaces/legalos/src/collections/SharedLegalTemplates.ts. Line 3 imports only `auditAfterChange` (not `auditAfterDelete`). Lines 33-35 contain `hooks: { afterChange: [auditAfterChange], }` with no `afterDelete`. Line 31 grants delete: `delete: isSuperAdmin`, so deletes are reachable. The `auditAfterDelete` hook exists and is exported at src/hooks/audit.ts line 41 (`export const auditAfterDelete: CollectionAfterDeleteHook = ...`). grep across src/collections/ confirms every other auditable collection pairs both hooks: Sites.ts:35, Quizzes.ts:22, Leads.ts:20, Numbers.ts:21, Users.

### 43. Sites read access maps raw binding values that may be populated objects into an id `in` filter
- **Where:** `src/collections/Sites.ts:14-21`  ·  **Subsystem:** Collections schema correctness  ·  **Category:** tenant-isolation  ·  **Reviewer confidence:** low
- **Problem:** The Sites `read` access builds `{ id: { in: ids } }` from `user.siteBindings?.map((b) => b.site)`. As with the update rule, `b.site` on a populated auth user is often the object `{ id, name, slug }`, not a raw id. The local type annotation here (`siteBindings?: Array<{ site: string | number }>`) assumes a raw id and contradicts the canonical AuthedUser type in src/lib/auth.ts:10 which types `site` as `{ id; name; slug } | string | number` precisely because it can be populated. If the array of objects is passed into `{ id: { in: [...] } }`, non-super-admin operators can fail to see the Sites they are bound to (empty list), whereas the rest of the codebase normalizes this shape. This is the same root cause as the update finding and should be fixed together.
- **Fix:** Map to the underlying id with the object-or-id normalization used in src/lib/auth.ts: `const ids = (user.siteBindings ?? []).map((b) => (typeof b.site === 'object' && b.site ? (b.site as { id: unknown }).id : b.site))` before building the `{ id: { in: ids } }` filter.
- **Verifier:** The reviewer's snippet matches the code exactly. src/collections/Sites.ts:14-21 read access does: `const ids = user.siteBindings?.map((b) => b.site) ?? []` then `if (ids.length === 0) return false; return { id: { in: ids } }`, with the local type annotation (line 15) `siteBindings?: Array<{ site: string | number }>` assuming a raw id. There is no object-or-id normalization. The same un-normalized pattern appears in the update rule (line 27) and in siteScopedRead/Write/Admin (src/access/index.ts:23 `.map((b) => b.site)`).

The claim that `b.site` can be a populated object is directly corroborat

### 44. saveIntegrationConfig saves SMTP port 0 when the port field is cleared (empty-string short-circuits ?? and Number('') === 0)
- **Where:** `src/app/(app)/admin/(top)/settings/integrations/actions.ts:39`  ·  **Subsystem:** Admin server actions (sites/users/settings)  ·  **Category:** data-integrity  ·  **Reviewer confidence:** high
- **Problem:** The Port input is a real form field that is always submitted. If the operator clears it, `formData.get('smtp_port')` returns '' (empty string, not null), so `'' ?? 587` evaluates to '' (nullish coalescing only falls through on null/undefined), and `Number('')` is 0. The config is then persisted with `smtp.port = 0` instead of the intended default 587, silently breaking outbound SMTP. The `?? 587` fallback only protects the (never-occurring) absent-field case.
- **Fix:** Parse defensively: `const port = Number(formData.get('smtp_port')); data.smtp.port = Number.isFinite(port) && port > 0 ? port : 587`.
- **Verifier:** The cited code at src/app/(app)/admin/(top)/settings/integrations/actions.ts:39 is verbatim: `port: Number(formData.get('smtp_port') ?? 587),`. The port field is a real, always-submitted form input — IntegrationsForm.tsx:51 renders `<input name="smtp_port" type="number" defaultValue={initial.smtp.port} className={inputClass} />` inside the `<form action={onSubmit}>`. When an operator clears it, `formData.get('smtp_port')` returns `''` (empty string, not null), so the `?? 587` guard does not fire (nullish coalescing only falls through on null/undefined). I confirmed via Node that `Number('' ?? 

### 45. signIn open-redirect guard allows backslash paths (e.g. /\evil.com) that browsers normalize to a protocol-relative external URL
- **Where:** `src/app/(auth)/sign-in/actions.ts:19`  ·  **Subsystem:** Admin server actions (sites/users/settings)  ·  **Category:** security  ·  **Reviewer confidence:** medium
- **Problem:** The post-login redirect comes from the client-supplied `?redirect=` query param (sign-in page.tsx passes it into the form, signIn returns it, and SignInForm does `router.replace(result.redirect)`). The safety check is `requestedRedirect.startsWith('/') && !requestedRedirect.startsWith('//')`. A value like `/\evil.com` (or `/\/evil.com`) starts with '/' and does NOT start with '//', so it passes — but browsers normalize backslashes to forward slashes in URLs, turning `/\evil.com` into `//evil.com`, a protocol-relative URL that navigates to the external host evil.com. The identical guard exists in sign-in/page.tsx line 11, so both paths are affected. This enables a post-auth open redirect for phishing.
- **Fix:** Reject backslashes too, e.g. require `/^\/[^/\\]/.test(requestedRedirect)` (starts with a single '/', second char is neither '/' nor '\\'), or parse with `new URL(requestedRedirect, base)` and verify the resolved origin matches.
- **Verifier:** The bug is real and reachable. Verified the full client-controllable chain:

1. src/app/(auth)/sign-in/page.tsx line 7 reads `redirect?` from `searchParams` (client-supplied query param), then line 11 guards it: `const safeRedirect = redirectParam && redirectParam.startsWith('/') && !redirectParam.startsWith('//') ? redirectParam : '/admin/overview'`. Line 17 feeds it to server `redirect(safeRedirect)` (already-signed-in path), and line 44 passes it to `<SignInForm redirectTo={safeRedirect} />`.
2. src/app/(auth)/sign-in/SignInForm.tsx line 29 carries it in `<input type="hidden" name="redirect

### 46. saveAsSiteDefault reads arbitrary tenant's brand_identity via overrideAccess: true
- **Where:** `src/app/(app)/admin/sites/[slug]/pages/[id]/site-defaults-action.ts:45-49`  ·  **Subsystem:** Page-builder server actions  ·  **Category:** tenant-isolation  ·  **Reviewer confidence:** medium
- **Problem:** saveAsSiteDefault loads the target Site with findByID + overrideAccess: true using a client-supplied siteId, bypassing Sites read access. The subsequent update uses overrideAccess: false (Sites.update requires an admin binding), so the write itself is protected and an unauthorized caller's request will ultimately fail. However the read still fetches another tenant's full brand_identity into server memory for an unauthorized user; combined with the missing isBoundToSite guard it is an unnecessary cross-tenant read. Lower severity because the leaked object is not returned to the caller.
- **Fix:** Gate the action with isBoundToSite(user, args.siteId) at the top, and/or perform the read with overrideAccess: false so Sites read access applies.
- **Verifier:** Read the file verbatim — it matches the reviewer's snippet. In src/app/(app)/admin/sites/[slug]/pages/[id]/site-defaults-action.ts the action only checks `if (!user) return { ok: false, error: 'unauthenticated' }` (line 26) and never calls isBoundToSite. It then fetches the client-supplied site with overrideAccess bypassing Sites.read:

  const src = (await payload.findByID({
    collection: 'sites',
    id: args.siteId,
    overrideAccess: true,
  })) as { brand_identity?: Record<string, unknown> | null }   // lines 45-49

Sites.read (src/collections/Sites.ts:14-21) would normally restrict no

### 47. aiBulkSimplify silently returns the original (un-simplified) text on a length mismatch while reporting ok:true
- **Where:** `src/app/(app)/admin/(top)/advertorials/actions.ts:187-188`  ·  **Subsystem:** Funnel builder (authoring + deployment)  ·  **Category:** error-handling  ·  **Reviewer confidence:** medium
- **Problem:** When the model returns an array whose length does not equal the input count, the action discards the result and returns the ORIGINAL texts with `ok: true`. The caller therefore cannot distinguish 'simplified successfully' from 'AI returned a malformed result, nothing changed'. For a bulk-simplify action the user will believe the rewrite ran when it silently no-op'd. This is a deliberate safety fallback but it conflates failure with success.
- **Fix:** Return a signal when the fallback fires, e.g. `return { ok: false, error: 'AI returned a mismatched result; nothing was changed.' }` on length mismatch, or include a `changed: boolean` flag so the UI can surface that no rewrite occurred.
- **Verifier:** The cited code matches verbatim. In src/app/(app)/admin/(top)/advertorials/actions.ts:187-188:
  const result = Array.isArray(out.texts) && out.texts.length === texts.length ? out.texts : texts
  return { ok: true, texts: result }
On a length mismatch, the action discards the AI output, returns the ORIGINAL `texts`, and still reports `ok: true`. There is no `changed` flag or any signal distinguishing a real rewrite from the no-op fallback.

I traced the only caller, src/components/builder/advertorial/AdvertorialBuilderApp.tsx:2148-2159. It guards with `if (res?.ok && Array.isArray(res.texts) &

### 48. SmartDatePicker keeps stale internal year/month/day across consecutive smart_date questions (seeds local state only on mount, never remounted)
- **Where:** `src/components/builder/quiz/preview.tsx:106-124`  ·  **Subsystem:** Public check-my-claim / quiz / survey flow  ·  **Category:** react-state  ·  **Reviewer confidence:** medium
- **Problem:** SmartDatePicker seeds its internal year/month/day from the `value` prop ONLY in the useState initializers, then never syncs to prop changes. PreviewQuestionCard resets the parent `smartDate` state to {} on node.id change (line 180) but renders <SmartDatePicker value={smartDate} ...> with no `key` (line 253) and PreviewQuestionCard itself is rendered without a key in QuizPreviewView (line 442), so React reuses the same SmartDatePicker instance across question nodes. If a quiz has two consecutive smart_date questions (or the user navigates back to one), the picker shows the previously chosen year/month/day instead of an empty picker, and its internal effect can immediately re-emit the old date. The seed quiz only has one date step so it does not surface there, but author-built quizzes can hit it.
- **Fix:** Give the picker a stable per-node key (e.g. <SmartDatePicker key={node.id} ...>) so it remounts per question, or sync internal state to `value` via a useEffect when the value prop is cleared.
- **Verifier:** Every claim in the report matches the actual code in src/components/builder/quiz/preview.tsx.

1) SmartDatePicker seeds internal state ONLY in useState initializers, with no sync-on-prop-change effect (lines 107-109): `const [year, setYear] = useState((value || {}).year || '')`, `const [month, setMonth] = useState((value || {}).month || '')`, `const [day, setDay] = useState((value || {}).day || '')`. The only useEffect (lines 120-124) emits outward via onChange; nothing reads the incoming `value` prop after mount.

2) PreviewQuestionCard resets parent smartDate to {} on node.id change (line 18

### 49. Redis health check leaks the connection when ping() throws
- **Where:** `src/lib/system-health/checks.ts:190-196`  ·  **Subsystem:** System health / self-check / media / misc API  ·  **Category:** error-handling  ·  **Reviewer confidence:** high
- **Problem:** checkRedis connects an ioredis client and only calls client.quit() inside the time() callback, after a successful ping(). If client.connect() succeeds but client.ping() (or the await) rejects, control jumps to the outer catch and client.quit() is never reached, leaving an open, connected socket that will keep retrying/lingering. Because runSystemReport re-runs on every cache miss (30s TTL) and can be force-refreshed, repeated transient ping failures slowly accumulate leaked Redis connections.
- **Fix:** Wrap the connect/ping in try/finally and call client.quit() (or client.disconnect()) in the finally, so the client is always torn down regardless of whether ping() succeeds.
- **Verifier:** The cited code at src/lib/system-health/checks.ts:191-196 matches the reviewer's snippet exactly:

    const { value, duration_ms } = await time(async () => {
      await client.connect()
      const pong = await client.ping()
      await client.quit()
      return pong
    })

The client is created with `new Redis(url, { maxRetriesPerRequest: 1, lazyConnect: true, connectTimeout: 2000 })` (line 190). With `lazyConnect: true`, `await client.connect()` (line 192) opens a real socket. `client.quit()` is only invoked at line 194, AFTER `client.ping()` (line 193) resolves.

I confirmed the `time` 


## NIT (1)

### 50. Invalid publish_at fails the whole page save with an unhelpful generic error
- **Where:** `src/app/(app)/admin/sites/[slug]/pages/[id]/blocks-actions.ts:88`  ·  **Subsystem:** Page-builder server actions  ·  **Category:** error-handling  ·  **Reviewer confidence:** high
- **Problem:** publish_at is computed as `new Date(args.publish_at).toISOString()`. If the client sends a non-empty but unparseable date string, new Date(...) is Invalid Date and .toISOString() throws RangeError. This is caught by the outer try/catch and surfaced as the generic 'save failed' message, so the entire body_blocks save is silently lost and the user gets no indication the date was the problem (unlike schema_json, which is validated with a specific message just above). Builder edits debounce-save on every burst, so a bad date in the publish_at field would make all subsequent autosaves fail.
- **Fix:** Validate explicitly: `const d = args.publish_at ? new Date(args.publish_at) : null; if (d && isNaN(d.getTime())) return { ok: false, error: 'Publish date is not a valid date.' }` and use d.toISOString() only when valid.
- **Verifier:** The evidence matches exactly. In src/app/(app)/admin/sites/[slug]/pages/[id]/blocks-actions.ts:88: `publish_at: args.publish_at ? new Date(args.publish_at).toISOString() : null,`. I verified in node that `new Date('not-a-date').toISOString()` throws `RangeError: Invalid time value`. This expression is evaluated during construction of the `data` object passed to `payload.update`, inside the try block (lines 41-110), before `publish_at` (line 88) is even followed by the schema_json IIFE (line 92). So a non-empty unparseable `publish_at` throws before `payload.update` runs, the throw is caught by


## UNCERTAIN (needs a human look)

- **Sites update access uses strict .includes() on un-normalized binding site values, can deny legitimate site admins** — `src/collections/Sites.ts:23-30`. The Sites `update` access compares the document id against `adminSiteIds` with `adminSiteIds.includes(id)`, where `adminSiteIds` is `user.siteBindings.filter(...).map((b) => b.site)`. Two problems make this comparison unreliable: (1) `siteBindings[].site` is a Payload relationship and the authenticated `req.user` is populated, so `b.site` is frequently a populated object `{ id, name, slug }` rathe

## REJECTED as false positives (recorded for transparency)

- _(works_as_intended)_ **HLR is_ported coerces missing data to false, conflating "not ported" with "unknown"** — `src/lib/integrations/hlr.ts`. The cited snippet is accurate: src/lib/integrations/hlr.ts line 57 is `is_ported: Boolean(r.ported),` and line 20 declares `is_ported?: boolean` (optional/tri-state). So the coercion does technically collapse a missing `ported` into a definitive `false`, losing the "unknown" distinction the type all
- _(false_positive)_ **Gallery and LogoCloud render <img> with possibly-undefined image_url (no per-item src guard)** — `src/components/blocks/BlockRenderer.tsx`. The renderer snippet is accurate: in BlockRenderer.tsx, Gallery maps `src={it.image_url}` (line 1046) and LogoCloud maps `src={l.image_url}` (line 1110), each guarded only by `if (images.length === 0) return null` (line 1027) / `if (logos.length === 0) return null` (line 1074), with no per-item url 
- _(false_positive)_ **Sites read/update access uses raw b.site and strict includes(), so site admins can't see or edit their own Site** — `src/collections/Sites.ts`. The claim hinges on `b.site` being a populated object in the access-function context. Read of the actual code refutes this.

Sites.ts:18-20 (read): `const ids = user.siteBindings?.map((b) => b.site) ?? []` then `return { id: { in: ids } }`. Sites.ts:27-29 (update): `const adminSiteIds = user.siteBin
- _(false_positive)_ **Audit log records an empty diff for create operations, losing the created content** — `src/hooks/audit.ts`. The reported root cause is a misreading of Payload's hook semantics. The claim states that on a create operation `previousDoc` is undefined, so `shallowDiff`'s line-6 guard `if (!before) return []` (src/hooks/audit.ts:5-7) fires and discards the created values.

That premise is false. In Payload CMS
- _(works_as_intended)_ **updateUser silently wipes all siteBindings when the submitted siteBindings JSON fails to parse** — `src/app/(app)/admin/(top)/settings/users/actions.ts`. The reviewer's snippet matches the code exactly. In actions.ts:22-40 `parseBindings` returns `[]` on JSON.parse failure (catch at 37-39), non-array payload (line 26), null/missing input (line 23), or all-invalid entries (filtered at 36); and updateUser (lines 107-110) unconditionally writes `siteBin
- _(works_as_intended)_ **cloneQuiz generates a copy slug with no DB uniqueness check (collision risk)** — `src/app/(app)/admin/(top)/quizzes/actions.ts`. The literal code is as cited. src/app/(app)/admin/(top)/quizzes/actions.ts:74 builds `slug: ${src.slug}-copy-${Date.now().toString(36).slice(-4)}` with no pre-create slug query, and src/collections/FunnelQuizzes.ts:28 is `{ name: 'slug', type: 'text', required: true, index: true }` — index, not uniq
- _(works_as_intended)_ **Thanks (DQ) page client Pixel event 'DQLead' has no matching server CAPI event, but is fired with eventID intended for dedupe** — `src/components/public/check-my-claim/Thanks.tsx`. I read Thanks.tsx in full. Line 25 matches the reviewer's snippet verbatim: `window.fbq?.('track', 'DQLead', {}, eventId ? { eventID: eventId } : undefined)`. The reviewer's factual claims are accurate: grep across all of src/ shows `DQLead` appears ONLY in Thanks.tsx (and its TikTok twin on line 28
import { Placeholder } from '@/components/app/Placeholder'
export default function Page() {
  return <Placeholder title="Users" sub="Per-Site role bindings (admin / editor / analyst)" deepLink={{ href: '/cms/collections/users', label: 'Manage users in raw admin' }} />
}

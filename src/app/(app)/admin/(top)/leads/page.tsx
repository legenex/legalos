import { Placeholder } from '@/components/app/Placeholder'
export default function Page() {
  return <Placeholder title="Leads" sub="Cross-site leads with mandatory Site filter" deepLink={{ href: '/cms/collections/leads', label: 'Manage leads in raw admin' }} />
}
